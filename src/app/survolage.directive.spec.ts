import { SurvolageDirective } from './survolage.directive';

describe('SurvolageDirective', () => {
  it('should create an instance', () => {
    const directive = new SurvolageDirective();
    expect(directive).toBeTruthy();
  });
});
